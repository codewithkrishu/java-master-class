# Level 12 — Lambda, Functional Interfaces & Streams

This level covers Java 8+ features including **Functional Interfaces, Lambda Expressions, Built-in Functional Interfaces, Method References, Comparator, Constructor References, Collections with Lambda, and Stream API**.

---

## 📚 Topics Covered

### 01. Functional Interface
- `@FunctionalInterface`
- Abstract method
- Default method
- Static method
- Lambda implementation

### 02. Lambda Parameters
- Lambda expressions with parameters
- Addition
- Multiplication

### 03. Lambda with Return Value
- Lambda expression returning a value
- Square of a number

### 04. Lambda with Boolean Return
- Boolean-returning Lambda
- Checking even numbers

### 05. Lambda with Multiple Statements
- Multiple statements inside Lambda
- Explicit `return`

### 06. Lambda with `forEach()`
- `forEach()`
- Lambda expression for collection traversal

### 07. Method Reference
- `::` method reference
- `System.out::println`

### 08. Predicate<T>
- `Predicate<T>`
- `test()`
- Boolean conditions

### 09. Consumer<T>
- `Consumer<T>`
- `accept()`
- Performs an action without returning a value

### 10. Supplier<T>
- `Supplier<T>`
- `get()`
- Supplies a value without taking input

### 11. Function<T, R>
- `Function<T, R>`
- `apply()`
- Takes one input and produces one output

### 12. BiFunction<T, U, R>
- Takes two inputs
- Produces one output
- `apply()`

### 13. BiPredicate<T, U>
- Takes two inputs
- Returns boolean
- `test()`

### 14. BiConsumer<T, U>
- Takes two inputs
- Performs an action
- `accept()`
- No return value

### 15. UnaryOperator<T> & BinaryOperator<T>
- `UnaryOperator<T>`
- One input → same type output
- `BinaryOperator<T>`
- Two inputs → same type output

### 16. Comparator + Lambda
- Sorting collections using Lambda
- Ascending order
- Descending order

### 17. String Sorting
- Sorting String objects
- `compareTo()`
- Lambda Comparator

### 18. Constructor Reference
- Constructor reference `::new`
- Using `Supplier<T>`
- Creating objects with method reference

### 19. Lambda + Collections
- `removeIf()`
- Removing elements using a condition
- Lambda with Collections

---

# 🌊 Stream API

### 20. Stream `filter()`
- Selecting elements based on a condition
- Filtering even numbers
- Filtering values greater than a limit

### 21. Stream `map()`
- Transforming elements
- Multiplication
- Squaring numbers

### 22. `filter()` + `map()`
- First filtering elements
- Then transforming selected elements
- Combining multiple Stream operations

### 23. Stream `sorted()`
- Sorting Stream elements
- Ascending order
- Descending order

### 24. Stream `collect()`
- Collecting Stream results
- Creating a new `List`
- `Collectors.toList()`

### 25. Stream `distinct()`
- Removing duplicate elements
- Getting unique values

### 26. Stream `limit()` & `skip()`
- `limit()` → controls how many elements are taken
- `skip()` → skips elements from the beginning
- Combining `skip()` and `limit()`

### 27. Stream `count()`
- Counting Stream elements
- Counting elements based on a condition

### 28. Stream `min()` & `max()`
- Finding minimum value
- Finding maximum value
- `Optional<T>`
- `get()`

### 29. Stream `reduce()`
- Combining multiple elements into one result
- Calculating total sum
- Accumulator concept

### 30. `anyMatch()`, `allMatch()`, `noneMatch()`
- `anyMatch()` → checks whether any element satisfies condition
- `allMatch()` → checks whether all elements satisfy condition
- `noneMatch()` → checks whether no element satisfies condition
- Returns `boolean`

### 31. `findFirst()` & `findAny()`
- Finding the first element
- Finding any element
- `Optional<T>`
- `get()`

---

## 🛠️ Java Concepts Practiced

- Functional Interfaces
- Lambda Expressions
- Default & Static Interface Methods
- Method References
- Constructor References
- Predicate
- Consumer
- Supplier
- Function
- BiFunction
- BiPredicate
- BiConsumer
- UnaryOperator
- BinaryOperator
- Comparator
- Collections
- Stream API
- `filter()`
- `map()`
- `sorted()`
- `collect()`
- `distinct()`
- `limit()`
- `skip()`
- `count()`
- `min()`
- `max()`
- `reduce()`
- `anyMatch()`
- `allMatch()`
- `noneMatch()`
- `findFirst()`
- `findAny()`

---

## 🎯 Goal of Level 12

The goal of this level is to build a strong understanding of **modern Java programming**, especially **Lambda Expressions, Functional Interfaces and Stream API**, which are widely used in real-world Java development.

---

## 📁 Programs

| No. | Program |
|---|---|
| 01 | Functional Interface |
| 02 | Lambda Parameters |
| 03 | Lambda Return Value |
| 04 | Lambda Boolean Return |
| 05 | Lambda Multiple Statements |
| 06 | Lambda with forEach() |
| 07 | Method Reference |
| 08 | Predicate |
| 09 | Consumer |
| 10 | Supplier |
| 11 | Function |
| 12 | BiFunction |
| 13 | BiPredicate |
| 14 | BiConsumer |
| 15 | UnaryOperator & BinaryOperator |
| 16 | Comparator + Lambda |
| 17 | String Sorting |
| 18 | Constructor Reference |
| 19 | Lambda + Collections |
| 20 | Stream filter() |
| 21 | Stream map() |
| 22 | filter() + map() |
| 23 | Stream sorted() |
| 24 | Stream collect() |
| 25 | Stream distinct() |
| 26 | limit() & skip() |
| 27 | Stream count() |
| 28 | min() & max() |
| 29 | Stream reduce() |
| 30 | anyMatch(), allMatch(), noneMatch() |
| 31 | findFirst() & findAny() |

---

### 🚀 Progress

**Level 12 — Completed ✅**

> Practicing Java step by step through problems and maintaining each level separately on GitHub.
